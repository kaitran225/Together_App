# Lombok config for common module
config.stopBubbling = true
# Default field modifier when none specified (omit "private" in source)
lombok.fieldDefaults.defaultPrivate = true
lombok.fieldDefaults.defaultFinal = false
