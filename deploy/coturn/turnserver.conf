listening-port=3478
tls-listening-port=5349
listening-ip=0.0.0.0
min-port=49152
max-port=49252

# coturn needs to know its own public IP to hand out correct relay candidates through NAT.
# NOTE: this is your current public IP — since it's a residential/dynamic IP, re-check it
# (curl https://api.ipify.org) if TURN stops working after a modem reboot / ISP re-lease,
# and update this line to match.
external-ip=115.75.211.213

fingerprint
lt-cred-mech
# Must match TURN_USERNAME / TURN_CREDENTIAL in your .env (also fed to the app via
# WEBRTC_TURN_USERNAME / WEBRTC_TURN_CREDENTIAL in docker-compose.yml). Edit both together.
user=testuser:35d9f66a7f77cd00fbf4e5cda46a2064
realm=together-app-exe201.duckdns.org

no-loopback-peers
no-multicast-peers
no-cli

log-file=stdout
simple-log
